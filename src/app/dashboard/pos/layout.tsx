import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'SM | Vente Comptoir',
}

export default function PosLayout({ children }: { children: React.ReactNode }) {
  return children
}
